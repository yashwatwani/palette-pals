# Build output — regenerate any time with ./build.sh
dist/

# The messy source folders: ~840 MB of which 422 MB is an aborted download and
# 236 MB is the same photos under different names. The deduplicated set of every
# real original lives in originals/ and IS committed.
images/
murals/
marriage/
workshop/
doodle/

# Junk
.DS_Store
._*
*.crdownload
node_modules/
.netlify/

# Stray personal documents; no PDF belongs in this static site
*.pdf
